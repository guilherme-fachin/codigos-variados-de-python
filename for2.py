#calculando a soma de uma sequencia de numeros usando um loop for
numeros = [1,2,3,4,5]
soma = 0

for numero in numeros:
    soma += numero
    #soma = soma + numero (é a mesma coisa da de cima so q a de cima resumida)

print("a soma dos numeros é:", soma)